// import { Logger as log } from '@utilities';

export class Elasticsearch {

    constructor() {
        this.init()
    }

    private init() {
        // const msg = log.loaderLog(`${log.yellow('Connecting to Elasticsearch...')}`);

        // setTimeout(() => {
        //     msg.succeed(`${log.green('Succefully eastablished connection to Elasticsearch')}`)
        // }, 10000);
    }

}